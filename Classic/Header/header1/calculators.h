#pragma once // It prevents the file from being created more than once.

int factorialCalc(int input); // This is how we create the skeleton of the function.
void piCalc(int input);

/* Global Variable */
extern double area;
extern double pi = 3.14;